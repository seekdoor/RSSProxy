version: '3.0'

services:
  rssproxy:
    image: allanpk716/rssproxy:latest
    volumes:
      - /mnt/user/appdata/rssproxy/config.yaml:/app/config.yaml
    ports:
      - 1201:1200
    restart: always